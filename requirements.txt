# LEELOO Device Requirements
# Install with: sudo pip3 install --break-system-packages -r requirements.txt
# NOTE: Must use system pip (not venv). See docs/FRESH_BUILD.md.

# Core display & rendering
Pillow>=10.0.0                          # PIL image processing (display rendering)
numpy>=1.24.0                           # Fast array ops for RGB565 framebuffer writes

# Network & communication
requests>=2.28.0                        # HTTP (Spotify, weather, geocode)
websockets>=12.0                        # WebSocket client (relay server)

# Setup & configuration
qrcode[pil]>=7.4.0                      # QR code generation (WiFi setup, Spotify OAuth)

# AI & voice
anthropic>=0.79.0                       # Claude Haiku intent router
# deepgram-sdk: NOT needed — leeloo_voice.py uses websockets directly to Deepgram API

# Raspberry Pi hardware (NeoPixel LEDs + ADXL345 accelerometer)
adafruit-circuitpython-neopixel>=6.3.0  # WS2812B LED control (GPIO 12 / PWM0)
adafruit-circuitpython-adxl34x>=1.12.0  # ADXL345 accelerometer (I2C, tap detection)
adafruit-blinka>=8.0.0                  # CircuitPython hardware abstraction layer

# I2C (ADXL345 accelerometer)
smbus2>=0.4.0                           # I2C communication library
